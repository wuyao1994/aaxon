spring:
  application:
    name: web-storefront
  cloud:
    config:
      uri: http://${DOCKER_IP}:8888
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream,bus-refresh,info,health